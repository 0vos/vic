"""Phase 1 初级版本：全局配置。

所有可调参数集中在这里，改完直接重跑 run.py 即可。
"""
from pathlib import Path

# ---------------------------------------------------------------------------
# 输入 / 输出
# ---------------------------------------------------------------------------
# 输入视频路径（命令行参数可覆盖）
VIDEO_PATH = "input.mp4"

# 输出目录
OUTPUT_DIR = Path("output")

# ---------------------------------------------------------------------------
# 抽帧
# ---------------------------------------------------------------------------
# 从视频里抽多少帧参与聚合（帧越多覆盖越全，但也越慢）
NUM_FRAMES = 24
# 抽帧方式：均匀间隔抽 NUM_FRAMES 帧
FRAME_SAMPLING = "uniform"

# ---------------------------------------------------------------------------
# Canonical 画布（最终聚合图的分辨率）
# ---------------------------------------------------------------------------
CANONICAL_WIDTH = 512
CANONICAL_HEIGHT = 768

# ---------------------------------------------------------------------------
# 分割（rembg）
# ---------------------------------------------------------------------------
# rembg 模型：u2net（通用）/ isnet-general-use（更精细但更慢）
REMBG_MODEL = "u2net"

# ---------------------------------------------------------------------------
# 可视化
# ---------------------------------------------------------------------------
# 是否保存中间过程图（每帧的 pose、分割、warp 结果）
SAVE_INTERMEDIATE = True
